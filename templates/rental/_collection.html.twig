{% block _rental_images_widget %}

	{{ form_widget(form) }}

	<input type="hidden" id="widget-counter" value="0">

	<div class="form-group">
		<button type="button" id="rental-image" class=" btn btn-primary">Ajoutez une image</button>
	</div><br>

{% endblock %}

{% block _rental_images_entry_row %}
	{{ form_widget(form) }}
{% endblock %}

{% block _rental_images_entry_widget %}
	<div class="form-group" id="block_{{id}}">
		<div class="row">
			<div class="col-10">
				<div class="row">
					<div class="col">
						{{ form_widget(form.url) }}
                        {{ form_errors(form.caption) }}
					</div>
					<div class="col">
						{{ form_widget(form.caption) }}
                        {{ form_errors(form.caption) }}
					</div>
					
				</div>
			</div><br><br><br>
			<div class="col-2">
				<button type="button" data-action="delete" data-target="#block_{{id}}" class="btn btn-danger fas fa-trash-alt"></button>
			</div>
		</div>
	</div>
{% endblock %}

